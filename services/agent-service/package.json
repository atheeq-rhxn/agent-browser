{
  "name": "@browser-agent/agent-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build index.ts --outdir dist",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@browser-agent/config": "workspace:*",
    "@types/bun": "latest",
    "@types/node": "^24.3.0",
    "typescript": "catalog:"
  }
}
